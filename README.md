# Project Name

## Description
A Django project using PostgreSQL, containerized with Docker for easy setup and deployment.

## Prerequisites

Before running the project, make sure you have the following tools installed:

- Docker
- Docker Compose

## Setup and Installation

1. **Clone the repository:**

   ```
   git clone https://github.com/Maksim124512M/MindStream.git
   cd project-name
   ```
2. **Create a .env file** <br>

   Copy .env.example to .env and update the database credentials.

3. **Build and start the containers:** <br>

   Run the following command to build and start the application:
   ```
   docker-compose up --build
   ```
